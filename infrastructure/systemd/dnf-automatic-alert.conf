# Drop-in for dnf-automatic.service (alpha-engine-config-I4493).
#
# A patch job that fails silently is the same defect class as boot-pull's
# broken reporter (I4509): the mechanism looks installed and is not working.
# The box already has the alert-on-failure@ handler pattern installed.
[Unit]
OnFailure=alert-on-failure@dnf-automatic.service.service
