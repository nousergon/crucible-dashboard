flow_name: dashboard
repo: nousergon/crucible-dashboard
owner: "@brianmcmahon"
notify:
  - type: email
    sender: ${EMAIL_SENDER}
    recipients: ${EMAIL_RECIPIENTS}
    smtp_host: smtp.gmail.com
    smtp_port: 587
    smtp_password: ${GMAIL_APP_PASSWORD}
  - type: github
    repo: nousergon/alpha-engine-config
    token: ${FLOW_DOCTOR_GITHUB_TOKEN}
    labels: [flow-doctor, area:dashboard]
    notify_on_category: [CODE, CONFIG]
  # Telegram forum-topic routing (config#1741 / config#1695 category paging).
  - type: telegram
    bot_token: ${TELEGRAM_BOT_TOKEN}
    chat_id: ${TELEGRAM_CHAT_ID}
    message_thread_id: ${FLOW_DOCTOR_TELEGRAM_THREAD_CRITICAL}
    parse_mode: Markdown
    disable_notification: false
    notify_on: [critical]
  - type: telegram
    bot_token: ${TELEGRAM_BOT_TOKEN}
    chat_id: ${TELEGRAM_CHAT_ID}
    message_thread_id: ${FLOW_DOCTOR_TELEGRAM_THREAD_OPS_HEALTH}
    parse_mode: Markdown
    disable_notification: false
    notify_on: [error, warning]
    notify_on_category: [TRANSIENT, EXTERNAL, INFRA]
  - type: s3
    bucket: alpha-engine-research
    subsystem: dashboard
store:
  type: sqlite
  path: /tmp/flow_doctor.db
dedup_cooldown_minutes: 60
dependencies:
  - boot-pull
rate_limits:
  max_alerts_per_day: 10
  max_issues_per_day: 3
  max_diagnosed_per_day: 3
