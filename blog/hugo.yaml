baseURL: https://nousergon.ai/blog/
locale: en-us
title: "Nous Ergon"
theme: PaperMod

# Build output goes to blog/public/, then marketing-apex's build:blog script
# copies it into marketing-apex/public/blog/ so Astro ships it as part of the
# nousergon.ai Cloudflare Pages deploy (see deploy-marketing.yml).
publishDir: public

paginate: 10

params:
  env: production
  title: "Nous Ergon"
  description: "Intelligence at work — building an autonomous alpha engine with AI"
  author: "Brian McMahon"

  # Dark mode by default, no toggle (matches site brand)
  defaultTheme: dark
  disableThemeToggle: true

  # Show reading time, TOC, breadcrumbs
  ShowReadingTime: true
  ShowBreadCrumbs: true
  ShowPostNavLinks: true
  # PaperMod's default share row is generic share-intent links (Twitter,
  # Facebook, Reddit, Telegram, WhatsApp) rather than Brian's own profiles —
  # off by request.
  ShowShareButtons: false
  ShowCodeCopyButtons: true
  ShowToc: true
  TocOpen: false

  # Comments enabled (Giscus configured in layouts partial)
  comments: true

  # Cover image defaults
  cover:
    linkFullImages: true

  # Default OG image for social sharing (Twitter/X, LinkedIn, etc.)
  images:
    - images/og-default.png

  # Assets — custom CSS for brand styling
  assets:
    disableFingerprinting: false

# Menu removed — custom header.html handles nav to match nousergon.ai

# Markdown rendering
markup:
  highlight:
    style: dracula
    lineNos: false
    guessSyntax: true
  goldmark:
    renderer:
      unsafe: true

# Output formats
outputs:
  home:
    - HTML
    - RSS

# Taxonomy
taxonomies:
  tag: tags
